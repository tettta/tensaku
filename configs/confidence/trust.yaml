# configs/confidence/trust.yaml
estimators:  # <--- この行が必要！
  - name: msp
  - name: entropy
  - name: trust
    version: v2
    metric: cosine
    k_list: [5]
    agg: median
    trim_q: 0.1
    normalize: zscore