# configs/data/base.yaml
# Data paths (Strict): no silent fallback.

data_root: data_sas

# REQUIRED: set per run (e.g., data.qid=Y14_1-2_1_3)
qid: ???

# Split outputs are stored under: {project_root}/{data_root}/q-{qid}/{split.subdir}/
base_dir: ${run.project_root}/${data.data_root}/q-${data.qid}/${split.subdir}

# Master dataset
input_all: ${run.project_root}/${data.data_root}/all.jsonl

files:
  labeled: labeled.jsonl
  dev: dev.jsonl
  test: test.jsonl
  pool: pool.jsonl

text_key_primary: mecab
label_key: score
id_key: id
max_len: 128
