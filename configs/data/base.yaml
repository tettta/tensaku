# configs/data/base.yaml

qid: ${qid.value}
data_root: data_sas
splits_dir: splits

# 【修正】シードごとにフォルダを分けるロジック
# run.project_root を起点にするため、どこから実行してもリンク切れしない
base_dir: ${project_root}/${data.data_root}/${data.splits_dir}/${data.qid}/seed_${run.seed}

# 【修正】マスタデータもルート起点で指定
input_all: ${project_root}/${data.data_root}/all.jsonl

# 以下、変更なし
files:
  labeled: labeled.jsonl
  dev: dev.jsonl
  test: test.jsonl
  pool: pool.jsonl
text_key_primary: mecab
label_key: score
id_key: id
max_len: 128