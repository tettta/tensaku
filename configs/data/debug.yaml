# configs/data/base.yaml
qid: ${qid.value}

data_root: data_sas
splits_dir: splits_debug

# base split（不変・qid固定）
base_dir: ${hydra:runtime.cwd}/${data_root}/${splits_dir}/${data.qid}

# マスタデータ
input_all: ${hydra:runtime.cwd}/${data_root}/all.jsonl

files:
  labeled: labeled.jsonl
  dev: dev.jsonl
  test: test.jsonl
  pool: pool.jsonl

text_key_primary: mecab
label_key: score
id_key: id
max_len: 128
